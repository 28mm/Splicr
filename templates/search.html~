<!doctype html>
<html>
  <head>
    <title>Splicr</title>

    {% include "includes.html" %}

  </head>
  <body>
    {% include "navbar.html" %}

    <div class="row">
      <div class="col-sm-6 col-sm-offset-3 col-md-6 col-md-offset-3 col-lg-6 col-lg-offset-3">
        <!-- Simple Search Results -->
        <h2>Album Results for: {{ terms }}</h2>
        <ol>
          {% for album in albums %}
          <li><a href="/album/{{album.uuid}}">{{album.title}}</a></li>
          {% endfor %}
        </ol>
      </div>
    </div>
  </body>
</html>
